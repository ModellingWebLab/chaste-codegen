# This project has no generated cell-model sources of its own (see the sibling
# RefCheck_<scheme>_<model>_<variant> projects, generated by chaste_codegen/tests/numerical/generate_refcheck_harness.py,
# for those); it only reads the .dat outputs they produce and numerically diffs them.

find_package(Chaste COMPONENTS heart)

chaste_do_project(CodegenRefCheck)
