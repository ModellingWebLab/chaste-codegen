{% for entry in sparse_jacobian_entries -%}
rJacobian[ {{ loop.index0 }} ] = {% if membrane_voltage_index == entry.i %}mSetVoltageDerivativeToZero ? CHASTE_CONST(0.0) : ({{ entry.entry }});{% else %}{{ entry.entry }};{% endif %}
{% endfor -%}