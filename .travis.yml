language: python
python:
    - "3.6"
branches:
  only:
    - master
install:
    - pip install .[test]
    - pip install codecov
    # Temporary cellmlmanip install, until it can be done with pip
    - pip install -e git://github.com/ModellingWebLab/cellmlmanip.git#egg=cellmlmanip

script:
    - python -m flake8
    - python -m pytest --cov=weblab_cg weblab_cg
after_success:
    - codecov
